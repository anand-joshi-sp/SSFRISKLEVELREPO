# -------------------------------
# CAEP SSF Transmitter Configuration
# -------------------------------

# 1️⃣ Private Key for signing Security Event Tokens (SETs)
# Must be a PKCS#8 RSA private key.
# The below key is for testing only — replace it in production.
PRIVATE_KEY_PEM=-----BEGIN PRIVATE KEY-----\nMIIEvAIBADANBgkq\n-----END PRIVATE KEY-----
# 2️⃣ API Token used for authenticating to receiver APIs
API_TOKEN=Bearer test-api-token-12345

# 3️⃣ Transmitter issuer (this service's URL)
ISS=http://localhost:3000/

# 4️⃣ Receiver audience
AUD=https://receiver.example.com/

# 5️⃣ Default receiver endpoint (use webhook.site for testing)
DEFAULT_RECEIVER_URL=https://webhook.site/<your-webhook-id>
